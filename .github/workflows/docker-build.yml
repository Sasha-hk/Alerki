name: Build

on:   
  # push:
  #   branches:
  #     - main

  #   tags:
  #     - v*

  pull_request:

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: . ./docker/build-test.sh
